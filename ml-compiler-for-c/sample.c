// simple c program to test my c compiler (hello world is way more complex than this)
int main() {
    int x = 5;
    int y = 10;
    int result = x + y;

    if (result > 10) {
        return result;
    } else {
        return 0;
    }
}
